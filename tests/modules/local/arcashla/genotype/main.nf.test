nextflow_process {

    name "Test Process ARCASHLA_GENOTYPE"
    script "modules/local/arcashla/genotype/main.nf"
    process "ARCASHLA_GENOTYPE"

    tag "modules"
    tag "modules_local"
    tag "arcas_hla/genome_generate"

    test("ARCASHLA_GENOTYPE") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results" //params.outputDir
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of([
                    [id: "test_1"],
                    [ file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_1.fastq.gz', checkIfExists: true) ],
                    [file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_2.fastq.gz', checkIfExists: true) ]
                ])
                """
            }
        }

        then {
            assertAll (
            //{assert path("$outdir/results").exists()},
            {assert process.success},
	        {assert process.out.alignment_p},
            { assert process.out.alignment_p},
	        {assert process.out.gt_log},
	        {assert process.out.gt_json},
	        {assert process.out.genes_json},
	        {assert process.out.versions},
            {assert snapshot(process.out)}
            )
        }
    }

}

