nextflow_process {

    name "Test Process TXIMPORT"
    script "modules/local/tximport/main.nf"
    process "TXIMPORT"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.fromPath("$projectDir/tests/test_result/tx2gene/quants/test")
                input[1] = Channel.fromPath("$projectDir/tests/test_result/tx2gene.tsv")
                input[2] = params.quant_type

                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
