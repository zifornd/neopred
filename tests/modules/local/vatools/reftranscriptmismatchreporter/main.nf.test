nextflow_process {

    name "Test Process VATOOLS_REFTRANSCRIPTMISMATCHREPORTER"
    script "modules/local/vatools/reftranscriptmismatchreporter/main.nf"
    process "VATOOLS_REFTRANSCRIPTMISMATCHREPORTER"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
		input[0] = Channel.of([
                    [ id:'test' ], 
                    [ file("$projectDir/tests/test_result/gunzip/test.annotated.vcf", checkIfExists: true) ]
                ])
		
                """
            }
        }

        then {
            assert process.success
	    assert process.out.filter_vcf
	    assert process.out.versions
            assert snapshot(process.out).match()
        }

    }

}
