nextflow_process {

    name "Test Process VATOOLS_VCFEXPRESSIONANNOTATOR"
    script "modules/local/vatools/vcfexpressionannotator/main.nf"
    process "VATOOLS_VCFEXPRESSIONANNOTATOR"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
		input[0] = Channel.of([
                    [ id:'SRR8281226' ], 
                    [ file("$projectDir/tests/test_result/gunzip/SRR8281226.annotated.vcf", checkIfExists: true) ]
                ])

		   input[1] = file("$projectDir/tests/test_result/batch_removal/design_batch_tpm.genesymbol.batchremoved.csv")

                """
            }
        }

        then {
            assert process.success 
	    assert process.out.expr_vcf
	    assert process.out.versions
            assert snapshot(process.out).match()
        }

    }

}
