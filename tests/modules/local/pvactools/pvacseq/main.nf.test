nextflow_process {

    name "Test Process PVACTOOLS_PVACSEQ"
    script "modules/local/pvactools/pvacseq/main.nf"
    process "PVACTOOLS_PVACSEQ"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
		input[0] = Channel.of([
                    [ id:'SRR8281226' ], 
                    [ file("$projectDir/tests/test_result/vcfexpressionannotator/SRR8281226.mutect2.somatic.base.snp.Somatic.hc.filter.vep.gx.vcf", checkIfExists: true) ]
                ])
		input[1] = Channel.of([
                    [ id:'SRR8281226' ], 
                    [ file("$projectDir/tests/test_result/gunzip/SRR8281226.annotated.vcf", checkIfExists: true) ]
                ])

	        input[2] = "MHCflurry"
	        input[3] = "9"

                """
            }
        }

        then {
            assert process.success
	    assert process.out.versions
            assert snapshot(process.out).match()
        }

    }

}
