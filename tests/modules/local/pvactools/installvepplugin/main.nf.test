nextflow_process {

    name "Test Process PVACTOOLS_INSTALLVEPPLUGIN"
    script "modules/local/pvactools/installvepplugin/main.nf"
    process "PVACTOOLS_INSTALLVEPPLUGIN"

    test("Should run PVACTOOLS_INSTALLVEPPLUGIN without failures") {

        when {
            process {
                """
                //input[0] = file("$projectDir/tests/test_result/local/quants/test_input_file.pm")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert process.out.results },
                { assert process.out.versions },
		{assert snapshot(process.out).match() }
            )
        }

    }

}
