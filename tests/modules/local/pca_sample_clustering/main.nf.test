nextflow_process {

    name "Test Process PCA_SAMPLE_CLUSTERING"
    script "modules/local/pca_sample_clustering/main.nf"
    process "PCA_SAMPLE_CLUSTERING"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = file("$projectDir/assets/samplesheet.csv")
                input[1] = params.batch
                input[2] = params.design
                input[3] = file("$projectDir/tests/test_result/design_batch_tpm.genesymbol.batchremoved.csv")
                input[4] = file("$projectDir/tests/test_result/design_batch_tpm.genesymbol.csv")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out)//.match()
        }

    }

}
