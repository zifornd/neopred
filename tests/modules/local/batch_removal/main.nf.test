nextflow_process {

    name "Test Process BATCH_REMOVAL"
    script "modules/local/batch_removal/main.nf"
    process "BATCH_REMOVAL"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = file("${projectDir}/assets/samplesheet.csv")
                input[1] = params.batch
                input[2] = params.design
                input[3] = file("${projectDir}/tests/test_result/salmon.merged.gene_tpm.tsv")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
