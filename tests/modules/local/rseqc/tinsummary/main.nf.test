nextflow_process {

    name "Test Process RSEQC_TINSUMMARY"
    script "modules/local/rseqc/tinsummary/main.nf"
    process "RSEQC_TINSUMMARY"

    test("rseqc tin summary for bam files") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of([
			   [id: 'test'],
			   file("$projectDir/tests/results/test.paired_end.sorted.summary.txt")
])
                """
            }
        }

        then {
            assert process.success
            assert process.out.summary_txt
	    assert process.out.versions
            assert snapshot(process.out).match()
        }

    }

}
